---
"@zitadel/server": minor
---

Add POST /grants/query to list a project's collaboration grants, with always-on user-ref or team-ref labels and an opt-in `expand: ["principal"]` that embeds the GET user or GET team body.
