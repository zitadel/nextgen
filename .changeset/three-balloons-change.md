---
"@zitadel/shared-component-styles": patch
"@zitadel/components": patch
"@zitadel/ui-react": patch
"@zitadel/storybook": patch
---

Refactors the Liquid field rendering system into a dedicated partial and module, renames mandatory-gates → field-patcher, fixes select enum values being dropped by the Go API, and documents the real-server demo workflow.
