---
---

Internal OpenFGA authz parser, profile validator, and compiler. Nothing imports
`internal/authz` yet, so the shipped binary is unchanged. Customer-facing release
notes belong with the upload/check surface (#422).
