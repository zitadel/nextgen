---
"@zitadel/server": minor
---

User create, get, and delete, team create and update, and the schema, login flow, and branding reads accept a Console session cookie as the signed-in user, so the embedded Console's management screens load without a project secret.
