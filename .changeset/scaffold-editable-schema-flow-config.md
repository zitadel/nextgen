---
"@zitadel/cli": minor
"@zitadel/api": minor
"@zitadel/config": minor
"@zitadel/server": minor
---

Scaffold editable schema and flow config from shared local defaults, add project default seeding control, and seed sync state so plan is idempotent immediately after setup.
