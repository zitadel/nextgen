---
"@zitadel/components": patch
"@zitadel/shared-component-styles": patch
---

Expose `--zl-page-min-height` so host pages can size the embedded login widget to content (`auto`) instead of the default full-viewport page shape; releases both the orchestrator mount and the `zl-page-shell` atom.
