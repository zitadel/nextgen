---
"@zitadel/components": patch
"@zitadel/server": patch
---

Security: update DOMPurify in `@zitadel/components` and gRPC in `@zitadel/server`, and refresh vulnerable workspace dependencies tracked by Dependabot.
