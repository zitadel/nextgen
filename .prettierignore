# Dependencies
node_modules

# Build output (per-package generated dirs)
# Most SDK packages emit `dist`; sdk-qwik emits `lib` + `lib-types`.
dist
build
out
lib
lib-types
out-tsc

# TypeScript incremental build info
*.tsbuildinfo

# Framework build outputs
.next
.nuxt
.nuxt-typecheck
.output
.svelte-kit

# Test / coverage artifacts
coverage
test-output

# Lockfile (do not reformat)
pnpm-lock.yaml
