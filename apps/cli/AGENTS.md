# Zitadel CLI Agent Contract

Agents should run `zitadel <command> --non-interactive --json` and read the JSON envelope. This file is generated by `apps/cli/scripts/gen-agents-md.ts` from the oclif command metadata; do not edit it by hand.

## Required Agent Flags

```sh
npx zitadel@latest <command> --non-interactive --json
```

Use `--cwd <path>` when acting outside the current working directory. Discover commands and flags with:

```sh
npx zitadel@latest --help
npx zitadel@latest <command> --help
```

Agents should prefer `next_commands` over free-text hints.

## Maintainer Notes

This file is packaged with the CLI and is generated from the oclif command metadata. When changing commands, flags, envelope fields, server resolution, or renderer behavior, update the command classes and tests first, then run `corepack pnpm nx run @zitadel-nextgen/cli:gen:agents-md`.

Do not edit the generated contract block in this file by hand.

## Golden Path

The supported POC path is Next.js App Router setup, local mock/dev auth, and config plan/apply.

Setup creates `zitadel.json`, `.zitadel/secret`, schema and flow resources under `.zitadel/`, browser-safe env metadata, and framework routes that mount `ZitadelFlow`. Project and preview secrets stay out of browser runtime.

## Renderer Direction

Generated routes use `ZitadelFlow({ purpose, projectId, issuer, environment })` so the future `<zitadel-flow>` web component can replace the React shim without changing the app-level contract.

## CLI vs. Runtime API

This CLI manages the dev-owned shape of a Zitadel deployment: schemas and flows. For per-customer-org configuration (e.g. a B2B customer's own SSO), end-user CRUD, or any unbounded set, route to the runtime Admin/Org API instead — not `zitadel` commands or `.zitadel/` files. Subordinate config (claim mappings, redirect URIs, role bindings) lives wherever its parent resource lives. See `docs/design/cli/README.md` § *What lives in `.zitadel/`* for the ownership rule and the resource-by-resource split.

<!-- generated:contract:begin -->

Every envelope carries `cli_version`, `command`, `source` at the top level.

## Commands

| Command | Summary |
|---|---|
| `zitadel setup` | Create a Zitadel project and scaffold local auth. |
| `zitadel plan` | Validate config without mutation and preview the sync diff. |
| `zitadel apply` | Validate and upload repo config to the platform. |
| `zitadel doctor` | Verify generated files and local state. |
| `zitadel status` | Summarize the local project state. |
| `zitadel eject` | Remove managed files and local Zitadel state. |

### `zitadel setup`

Create a Zitadel project and scaffold local auth.

| Flag | Type | Description |
|---|---|---|
| `--auth-method` | `string` | Auth method: passkey (default) or password. |
| `--cwd` / `-c` | `string` | Project directory to operate on. |
| `--debug` | `boolean` | Debug logging. |
| `--dry-run` | `boolean` | Preview without mutating files or the platform. |
| `--force` / `-f` | `boolean` | Overwrite protected files on conflict. |
| `--framework` | `string` | Framework to target (v1 supports "next"). |
| `--json` | `boolean` | Emit the JSON envelope instead of pretty output. |
| `--no-apply` | `boolean` | Skip the automatic apply at the end of setup. |
| `--non-interactive` / `-n` | `boolean` | Disable prompts. Required when scripting or running as an agent. |
| `--renderer` | `string` | Renderer: react (default) or web-component. |
| `--server` / `-s` | `string` | Override the resolved server URL. |
| `--user-fields` | `string` | Comma-separated list of user fields. |
| `--verbose` | `boolean` | Verbose logging. |

### `zitadel plan`

Validate config without mutation and preview the sync diff.

| Flag | Type | Description |
|---|---|---|
| `--cwd` / `-c` | `string` | Project directory to operate on. |
| `--debug` | `boolean` | Debug logging. |
| `--dry-run` | `boolean` | Preview without mutating files or the platform. |
| `--environment` / `-e` | `string` | Target environment (default: development). |
| `--force` / `-f` | `boolean` | Overwrite protected files on conflict. |
| `--json` | `boolean` | Emit the JSON envelope instead of pretty output. |
| `--non-interactive` / `-n` | `boolean` | Disable prompts. Required when scripting or running as an agent. |
| `--server` / `-s` | `string` | Override the resolved server URL. |
| `--verbose` | `boolean` | Verbose logging. |

### `zitadel apply`

Validate and upload repo config to the platform.

| Flag | Type | Description |
|---|---|---|
| `--cwd` / `-c` | `string` | Project directory to operate on. |
| `--debug` | `boolean` | Debug logging. |
| `--dry-run` | `boolean` | Preview without mutating files or the platform. |
| `--environment` / `-e` | `string` | Target environment (default: development). |
| `--force` / `-f` | `boolean` | Overwrite protected files on conflict. |
| `--json` | `boolean` | Emit the JSON envelope instead of pretty output. |
| `--non-interactive` / `-n` | `boolean` | Disable prompts. Required when scripting or running as an agent. |
| `--server` / `-s` | `string` | Override the resolved server URL. |
| `--verbose` | `boolean` | Verbose logging. |

### `zitadel doctor`

Verify generated files and local state.

| Flag | Type | Description |
|---|---|---|
| `--cwd` / `-c` | `string` | Project directory to operate on. |
| `--debug` | `boolean` | Debug logging. |
| `--dry-run` | `boolean` | Preview without mutating files or the platform. |
| `--fix` | `boolean` | Re-apply missing managed files. |
| `--force` / `-f` | `boolean` | Overwrite protected files on conflict. |
| `--json` | `boolean` | Emit the JSON envelope instead of pretty output. |
| `--non-interactive` / `-n` | `boolean` | Disable prompts. Required when scripting or running as an agent. |
| `--server` / `-s` | `string` | Override the resolved server URL. |
| `--verbose` | `boolean` | Verbose logging. |

### `zitadel status`

Summarize the local project state.

| Flag | Type | Description |
|---|---|---|
| `--cwd` / `-c` | `string` | Project directory to operate on. |
| `--debug` | `boolean` | Debug logging. |
| `--dry-run` | `boolean` | Preview without mutating files or the platform. |
| `--force` / `-f` | `boolean` | Overwrite protected files on conflict. |
| `--json` | `boolean` | Emit the JSON envelope instead of pretty output. |
| `--non-interactive` / `-n` | `boolean` | Disable prompts. Required when scripting or running as an agent. |
| `--server` / `-s` | `string` | Override the resolved server URL. |
| `--verbose` | `boolean` | Verbose logging. |

### `zitadel eject`

Remove managed files and local Zitadel state.

Aliases: `zitadel uninstall`

| Flag | Type | Description |
|---|---|---|
| `--cwd` / `-c` | `string` | Project directory to operate on. |
| `--debug` | `boolean` | Debug logging. |
| `--dry-run` | `boolean` | Preview without mutating files or the platform. |
| `--force` / `-f` | `boolean` | Overwrite protected files on conflict. |
| `--json` | `boolean` | Emit the JSON envelope instead of pretty output. |
| `--non-interactive` / `-n` | `boolean` | Disable prompts. Required when scripting or running as an agent. |
| `--server` / `-s` | `string` | Override the resolved server URL. |
| `--verbose` | `boolean` | Verbose logging. |

## Exit codes

| Code | Error code(s) |
|---:|---|
| 0 | `E_ALREADY_INIT` |
| 1 | `E_AUTH` |
| 2 | `E_NOT_IMPLEMENTED` |
| 3 | `E_FRAMEWORK_NOT_DETECTED`, `E_UNSUPPORTED_PROJECT_SHAPE`, `E_VALIDATION` |
| 4 | `E_NETWORK` |
| 5 | `E_CONFLICT` |

## Server resolution

Precedence (highest wins):

1. `--server <url|mock>` flag
2. `ZITADEL_API_BASE` env var
3. `zitadel.json#environments.<env>.server`
4. `zitadel.json#server`
5. Default: `https://api.zitadel.cloud`

The envelope `source` reports the resolved value (a URL or the literal `mock`).

<!-- generated:contract:end -->
