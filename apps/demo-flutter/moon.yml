id: "demo-flutter"
# Moon has no Dart toolchain; tasks run as system commands and expect the
# Flutter SDK on PATH (CI installs it via subosito/flutter-action).
language: "dart"
layer: "application"
stack: "frontend"
dependsOn:
  - "sdk-flutter"

tasks:
  lint:
    command: "dart format --output=none --set-exit-if-changed lib"
    inputs:
      - "lib/**/*"
      - "pubspec.yaml"
      - "analysis_options.yaml"

  typecheck:
    command: "flutter pub get && flutter analyze"
    options:
      shell: true
    deps:
      - "sdk-flutter:typecheck"
    inputs:
      - "lib/**/*"
      - "pubspec.yaml"
      - "analysis_options.yaml"

  test:
    command: "flutter pub get && flutter test"
    options:
      shell: true
    deps:
      - "sdk-flutter:test"
    inputs:
      - "lib/**/*"
      - "test/**/*"
      - "pubspec.yaml"

  # Local development only: `moon run demo-flutter:dev` (pick a device
  # with -d). Not part of the CI graph.
  dev:
    command: "flutter run"
    local: true
    options:
      persistent: true
      runInCI: false
