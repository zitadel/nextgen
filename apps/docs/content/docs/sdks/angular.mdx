---
title: Angular SDK
description: Angular wrappers for Zitadel login and logout UI.
---

Install manually when not using the CLI:

```sh
pnpm add @zitadel/sdk-angular
```

The Angular SDK exposes framework components that wrap the shared Zitadel login and logout elements. Configure the Zitadel project once and pass the handle to the rendered widgets.
