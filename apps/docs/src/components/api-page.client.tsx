"use client";

import { defineClientConfig } from "fumadocs-openapi/ui/client";

/**
 * Client-side configuration for the interactive API playground.
 * Handles try-it-out functionality in the browser.
 */
const client = defineClientConfig();

export default client;
