# Changelog

## 0.1.0-alpha.1

- Initial release: `FlowClient` (create/submit/current with the
  400-with-step unwrap), `SessionClient` (handoff exchange, me, revoke),
  hand-written models for the flow/session wire shapes with spec-lock
  tests against `api/openapi/`, `_zflow` cookie handling, bearer
  `session_token` storage, and built-in en/de/it locale dictionaries.
