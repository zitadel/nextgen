{
  "name": "@zitadel-nextgen/api-mock",
  "version": "0.0.0",
  "description": "Mock support library for @zitadel-nextgen/api",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./public-dir": {
      "types": "./src/public-dir.ts",
      "import": "./src/public-dir.ts"
    }
  },
  "bin": {
    "api-mock": "./bin/start.ts"
  },
  "scripts": {
    "start": "tsx bin/start.ts",
    "dev": "tsx watch bin/start.ts",
    "test": "vitest run --project unit",
    "test:browser": "vitest run --project browser",
    "test:all": "vitest run"
  },
  "dependencies": {
    "@mswjs/http-middleware": "catalog:",
    "@zitadel-nextgen/api": "workspace:*",
    "express": "catalog:",
    "xstate": "catalog:"
  },
  "peerDependencies": {
    "msw": "catalog:"
  },
  "devDependencies": {
    "@types/express": "catalog:",
    "@types/node": "catalog:",
    "@vitest/browser-playwright": "catalog:",
    "msw": "catalog:",
    "playwright": "catalog:",
    "tsx": "catalog:"
  },
  "nx": {
    "targets": {
      "build": {
        "executor": "nx:noop",
        "dependsOn": [
          "^build"
        ]
      }
    }
  },
  "msw": {
    "workerDirectory": [
      "public"
    ]
  }
}
