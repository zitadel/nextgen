id: "design-tokens"
language: "typescript"
layer: "library"
stack: "frontend"

tasks:
  sync:
    command: "corepack pnpm run sync"
    options:
      runInCI: false

  sync-export:
    command: "corepack pnpm run sync:export"

  generate:
    command: "corepack pnpm run generate"
    outputs:
      - "src/generated/tokens.css"
      - "src/generated/tokens.ts"
      - "src/generated/tailwind.css"
      - "src/generated/shadcn.css"

  lint:
    command: "corepack pnpm exec oxlint ."
    deps:
      - "generate"

  typecheck:
    command: "corepack pnpm exec tsc --build tsconfig.json"
    deps:
      - "generate"

  build:
    command: "corepack pnpm run build"
    deps:
      - "generate"
    outputs:
      - "dist/**/*"

  test:
    command: "corepack pnpm run test"
    deps:
      - "generate"

