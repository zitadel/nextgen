# Copy to .env next to docker-compose.yaml (see docs/quick-start/index.md).
# CI smokes this compose surface via scripts/deploy-smoke.mjs, overriding
# NEXTGEN_IMAGE with an image built from the release docker context.

NEXTGEN_IMAGE=ghcr.io/zitadel/nextgen:latest
NEXTGEN_PORT=8080
