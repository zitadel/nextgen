description: |
  The name of the environment that served this request, resolved from the
  request's `Origin`: a preview whose origins matched, else `live`.
schema:
  type: string
  example: preview-pr-42
