title: IdentifierFactorPayload
type: object
description: Identifier-specific factor metadata
required:
  - method
  - user_id
properties:
  method:
    type: string
    enum: [identifier]
    description: Always `identifier`; the discriminator of the payload union.
  user_id:
    $ref: ../../components/schemas/user-id.yaml
    description: The identified user ID
