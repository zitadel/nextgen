title: PasswordFactorPayload
type: object
description: Password authentication has no additional metadata beyond the base factor fields
required:
  - method
properties:
  method:
    type: string
    enum: [password]
    description: Discriminator for password factor payloads.
additionalProperties: false
