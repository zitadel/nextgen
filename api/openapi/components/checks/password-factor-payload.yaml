title: PasswordFactorPayload
type: object
description: |
  Password authentication carries no metadata beyond the discriminator the
  `payload` union resolves on.
additionalProperties: false
required:
  - method
properties:
  method:
    $ref: ../schemas/factor-method.yaml
    description: Always `password`; the discriminator of the payload union.
