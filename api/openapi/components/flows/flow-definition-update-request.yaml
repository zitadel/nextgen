type: object
required:
    - flow_definition
description: |
    Replaces the existing flow definition.
properties:
    schema_uri:
        $ref: ../../components/schemas/schema-uri.yaml
    flow_definition:
        $ref: flow-definition.yaml