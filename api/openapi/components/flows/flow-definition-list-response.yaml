type: object
required:
  - flow_definitions
properties:
  flow_definitions:
    type: array
    items:
      $ref: flow-definition-response.yaml # todo: or the detailed response?
  next_page_token:
    description: The token to retrieve the next page of results. Absent if there are no more results.
    type: string
    oneOf:
      - $ref: ../../components/schemas/page-token.yaml
      - type: 'null'