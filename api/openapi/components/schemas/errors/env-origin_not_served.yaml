# Code generated by gen_error_schemas; DO NOT EDIT.
description: |
  The environment does not serve the request origin. Register the origin on the environment (zitadel preview --origin, or the entry's issuer_pattern in zitadel.json) and deploy again.
  
  Error code: env.origin_not_served
allOf:
  - $ref: ../../error-details.yaml
  - type: object
    properties:
      code:
        type: string
        const: env.origin_not_served
    required:
      - code
