# Code generated; DO NOT EDIT.
description: |
  ErrRequestInvalid is returned when an incoming HTTP request fails structural validation (missing required fields, wrong types, failed regex, etc.) before it reaches domain logic.
  
  Error code: req.invalid
allOf:
  - $ref: ../../error-details.yaml
  - type: object
    properties:
      code:
        type: string
        const: req.invalid
    required:
      - code
