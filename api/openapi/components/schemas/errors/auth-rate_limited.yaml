# Code generated; DO NOT EDIT.
description: |
  ErrAuthRateLimited is returned when a caller has exceeded the allowed number of attempts (cross-cutting, not tied to a single resource).
  
  Error code: auth.rate_limited
allOf:
  - $ref: ../../error-details.yaml
  - type: object
    properties:
      code:
        type: string
        const: auth.rate_limited
    required:
      - code
