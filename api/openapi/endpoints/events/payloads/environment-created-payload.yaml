title: EnvironmentCreatedPayload
type: object
description: |
  Allowlisted fields for `environment.created`. The environment id is already
  the event's `entity_id`, so only the name and class are carried here.
properties:
  name:
    type: string
  class:
    type: string
    enum:
      - live
      - preview
