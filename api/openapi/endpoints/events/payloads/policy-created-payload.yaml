title: PolicyCreatedPayload
type: object
description: |
  Allowlisted fields for `policy.created`. Omits the `config` values.
properties:
  operation:
    type: string
  enforcement:
    type: string
  team_ids:
    type: array
    items:
      type: string
