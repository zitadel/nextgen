title: UserUpdatedPayload
type: object
description: |
  Payload for `user.updated` (deferred emitter). Same attribute rules as
  create: keys touched plus `x-audit` values only.
properties:
  attribute_keys:
    type: array
    items:
      type: string
  attributes:
    type: object
    additionalProperties: true
