# generated by gen_openapi_errors; DO NOT EDIT
description: Error response
content:
  application/json:
    schema:
      oneOf:
        - $ref: '../../../components/schemas/errors/auth-unauthorized.yaml'
        - $ref: '../../../components/schemas/errors/internal.yaml'
        - $ref: '../../../components/schemas/errors/req-invalid.yaml'
        - $ref: '../../../components/schemas/errors/sess-token_invalid.yaml'
        - $ref: '../../../components/schemas/errors/user-already_exists.yaml'
        - $ref: '../../../components/schemas/errors/user-conflict.yaml'
        - $ref: '../../../components/schemas/errors/user-invalid.yaml'
        - $ref: '../../../components/schemas/errors/user-not_found.yaml'
      discriminator:
        propertyName: code
        mapping:
          auth.unauthorized: '../../../components/schemas/errors/auth-unauthorized.yaml'
          internal: '../../../components/schemas/errors/internal.yaml'
          req.invalid: '../../../components/schemas/errors/req-invalid.yaml'
          sess.token_invalid: '../../../components/schemas/errors/sess-token_invalid.yaml'
          user.already_exists: '../../../components/schemas/errors/user-already_exists.yaml'
          user.conflict: '../../../components/schemas/errors/user-conflict.yaml'
          user.invalid: '../../../components/schemas/errors/user-invalid.yaml'
          user.not_found: '../../../components/schemas/errors/user-not_found.yaml'
    examples:
      auth.unauthorized:
        summary: auth.unauthorized
        value:
          code: auth.unauthorized
          message: 'The request lacks valid authentication credentials.'
      internal:
        summary: internal
        value:
          code: internal
          message: 'An unexpected error occurred.'
      req.invalid:
        summary: req.invalid
        value:
          code: req.invalid
          message: 'The request is invalid and fails base validation (missing required fields, wrong types, failed regex, etc.). Check the details for more information.'
      sess.token_invalid:
        summary: sess.token_invalid
        value:
          code: sess.token_invalid
          message: 'The session token is invalid (either malformed or expired).'
      user.already_exists:
        summary: user.already_exists
        value:
          code: user.already_exists
          message: 'a user already exists with the given unique attributes'
      user.conflict:
        summary: user.conflict
        value:
          code: user.conflict
          message: 'the user was modified concurrently, retry the request'
      user.invalid:
        summary: user.invalid
        value:
          code: user.invalid
          message: 'user invalid'
      user.not_found:
        summary: user.not_found
        value:
          code: user.not_found
          message: 'user not found'
