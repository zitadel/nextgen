type: object
required:
  - name
properties:
  name:
    type: string
    description: The name of the project.
  previewOrigins:
    type: array
    items:
      type: string
    description: |
      Origins which are allowed for previewing and testing the project.