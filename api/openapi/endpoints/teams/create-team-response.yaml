type: object
required:
  - id
  - createdAt
properties:
  id:
    type: string
    description: The unique identifier of the team.
  createdAt:
    type: string
    format: date-time
    description: The time when the team was created.
